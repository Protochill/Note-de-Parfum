# Note de Parfum — Каталог парфюмерии

**Название приложения:** Note de Parfum

**Краткое описание:**
«Note de Parfum» — это динамический веб-каталог для ценителей парфюмерии. Приложение позволяет пользователям изучать детальную информацию об ароматах, включая пирамиду нот, парфюмеров и историю создания. Зарегистрированные пользователи могут формировать личную коллекцию «Избранное», оставлять отзывы и получать персональные рекомендации на основе своих предпочтений. Проект реализован с упором на простоту, скорость загрузки и удобство использования.

**Целевая аудитория:**
- Люди, интересующиеся парфюмерией (от новичков до нишевых коллекционеров)
- Покупатели, которые хотят подробно изучить аромат перед покупкой в интернет-магазине
- Пользователи, ведущие цифровой дневник своих парфюмерных открытий

**Ключевой функционал:**
- Просмотр каталога парфюмерии с фильтрацией по брендам, годам выпуска и половой принадлежности
- Детальные карточки товаров с описанием пирамиды нот, характеристиками и отзывами
- Регистрация, аутентификация и восстановление пароля
- Личный кабинет пользователя с возможностью добавлять ароматы в избранное и писать отзывы
- Поиск по названию аромата или бренду
- Административная панель для управления каталогом (добавление/редактирование ароматов)

---

## Структура приложения (обновлённая)

| № | Раздел | Маршрут | Тип доступа | Описание |
|:--|:---|:---|:---|:---|
| 1 | Главная | `/` | Публичный | Промо-блок, популярные новинки, подборка редакции |
| 2 | Аутентификация | `/auth/*` (`/login`, `/register`, `/forgot-password`) | Публичный | Вход, регистрация, форма восстановления пароля |
| 3 | Каталог | `/catalog` | Публичный | Список всех ароматов с фильтрацией и поиском |
| 4 | Детальная страница аромата | `/catalog/:id` | Публичный | Полная информация об аромате |
| 5 | Бренды | `/brands` | Публичный | Список парфюмерных домов |
| 6 | Личный кабинет | `/profile` | Приватный | Настройки профиля, отзывы |
| 7 | Избранные | `/favorites` | Приватный | Список избранных ароматов |
| 8 | Корзина | `/cart` | Приватный | Список выбранных ароматов и оформление заказа |
| 9 | Админ-панель | `/admin` | Приватный (admin) | Управление пользователями и ароматами |

---

## Технологический стек и его обоснование

### Frontend (Клиентская часть)

**Язык и архитектура: Чистый JavaScript (Vanilla JS) + HTML + CSS**

Использование чистого JavaScript без фреймворков является осознанным решением, продиктованным масштабом проекта. Для компактного каталога парфюмерии внедрение тяжеловесных библиотек создало бы избыточную сложность.

**Преимущества подхода:**
- **Отсутствие лишних абстракций** — разработка ведется напрямую с DOM-деревом, обеспечивая полный контроль над интерфейсом
- **Легковесность и производительность** — финальный код не содержит кода фреймворка, что ускоряет загрузку страниц
- **Простота поддержки** — любой разработчик быстро разберется в структуре проекта

**Обмен данными с сервером: Fetch API**

Для асинхронного взаимодействия с бэкендом используется встроенный Fetch API. Это современный стандарт, позволяющий отправлять запросы без перезагрузки страницы. Отказ от библиотек вроде Axios обусловлен отсутствием необходимости в дополнительных зависимостях.

### Backend (Серверная часть)

**Среда выполнения: Node.js**

Node.js обеспечивает высокую производительность благодаря неблокирующей, событийно-ориентированной архитектуре. Ключевое преимущество — использование единого языка (JavaScript) на клиенте и сервере, что упрощает разработку и поддержку.

**Фреймворк: Express.js**

Express — минималистичный веб-фреймворк, предоставляющий удобные инструменты для маршрутизации и обработки запросов. Он не навязывает жесткой структуры, позволяя быстро создать легкое REST API под нужды проекта.

**База данных: MySQL**

MySQL оптимально подходит для хранения четких взаимосвязанных сущностей: пользователи, ароматы, бренды, отзывы. Это проверенное решение с высокой надежностью и простотой развертывания.

**Безопасность и сессии: bcrypt и express-session**

- **bcrypt** — обеспечивает надежное хеширование паролей с автоматической генерацией "соли", защищая от атак с использованием радужных таблиц
- **express-session** — управляет состоянием авторизации, храня на клиенте только уникальный идентификатор сессии

### Инфраструктура (Хостинг и деплой)

- **Frontend хостинг:** GitHub Pages / Vercel — автоматический деплой при пуше в репозиторий, бесплатные SSL-сертификаты, встроенная CDN
- **Backend хостинг и БД:** Render / Railway — управляемая инфраструктура, автоматическое управление окружением и масштабированием

---

## Визуальное проектирование интерфейса

### Страница: Главная (`/`)

**Тип доступа:** публичный

**Основные блоки:**

1. **Шапка:**
   - Логотип "Note de Parfum"
   - Навигация (Главная, Каталог, Бренды)
   - Иконки профиля и избранного
   - Поле поиска отсутствует

2. **Промо-блок (Hero):**
   - Баннер
   - Слоган
   - Кнопка "Перейти в каталог"

3. **Популярные ароматы:**
   - Горизонтальный список карточек

4. **Новинки:**
   - Последние добавленные ароматы

5. **Подборка редакции:**
   - Тематическая коллекция

6. **Футер:**
   - Контакты, соцсети, информация о проекте

**Динамический контент:**
- `GET /api/perfumes?sort=popular`
- `GET /api/perfumes?sort=new`

---

### Страница: Аутентификация (`/auth/*`)

**Тип доступа:** публичный

**Основные блоки:**

#### Вход (`/login`)
- Логин
- Пароль
- Кнопка "Войти"
- Ссылки: регистрация, восстановление пароля

#### Регистрация (`/register`)
- Логин
- Телефон
- Пароль
- Подтверждение пароля

#### Восстановление (`/forgot-password`)
- Ввод логина или телефона

**Динамика:**
- `POST /api/auth/*`

---

### Страница: Каталог (`/catalog`)

**Тип доступа:** публичный

**Основные блоки:**

1. **Шапка:**
   - Логотип
   - Навигация
   - Поле поиска 
   - Иконки профиля и избранного

2. **Сайдбар:**
   - Бренды
   - Пол
   - Год выпуска

3. **Основная область:**
   - Заголовок
   - Количество результатов
   - Сортировка
   - Сетка карточек

4. **Карточка аромата:**
   - Изображение
   - Название
   - Бренд
   - Рейтинг
   - Кнопка "В избранное"

5. **Пагинация**

6. **Футер**

**Динамический контент:**
- `GET /api/perfumes?q=...&brand=...&gender=...&year=...&sort=...`

---

### Страница: Детальная страница аромата (`/catalog/:id`)

**Тип доступа:** публичный

**Основные блоки:**

1. Изображение и информация
2. Пирамида нот
3. Описание
4. Отзывы
5. Рекомендации

**Динамический контент:**
- `GET /api/perfumes?q=...`
- `GET /api/reviews?perfumeId=...`

---

### Страница: Бренды (`/brands`)

**Тип доступа:** публичный

**Основные блоки:**

1. **Шапка (без поиска)**

2. **Список брендов:**
   - Логотип
   - Название
   - Страна
   - Год основания

3. **Карточка бренда:**
   - Описание
   - Кнопка "Смотреть ароматы"

4. **Фильтрация (опционально):**
   - По стране
   - По алфавиту

5. **Футер**

**Динамика:**
- `GET /api/perfumes`

---

### Страница: Личный кабинет (`/profile`)

**Тип доступа:** приватный

**Основные блоки:**

1. Информация о пользователе (логин, телефон)
2. Редактирование профиля
3. История отзывов
4. Навигация (включая ссылку на избранное)
5. Футер

**Динамика:**
- `GET /api/auth/profile`
- `PATCH /api/auth/profile`

---

### Страница: Избранные (`/favorites`)

**Тип доступа:** приватный

**Основные блоки:**

1. Шапка
2. Заголовок "Мои избранные ароматы"
3. Список карточек ароматов
4. Действия:
   - Удалить из избранного
   - Перейти к аромату
5. Пустое состояние:
   - Сообщение
   - Кнопка перехода в каталог
6. Футер

**Динамика:**
- `GET /api/favorites`
- `POST /api/favorites/:perfumeId/toggle`

---

### Страница: Корзина (`/cart`)

**Тип доступа:** приватный

**Основные блоки:**

1. Шапка
2. Заголовок "Корзина"
3. Список товаров в корзине:
   - Название аромата
   - Бренд
   - Объём
   - Цена
   - Количество
   - Сумма по позиции
4. Управление позицией:
   - Изменение количества
   - Удаление из корзины
   - Переход к карточке аромата
5. Итоговая сумма заказа
6. Действия заказа:
   - Очистить корзину
   - Оформить заказ
7. Пустое состояние:
   - Сообщение о пустой корзине
   - Кнопка перехода в каталог
8. Футер

**Динамика:**
- Нет отдельного API, корзина работает на клиенте с хранением в localStorage

---

### Страница: Админ-панель (`/admin`)

**Тип доступа:** приватный (admin)

**Основные блоки:**

1. Навигация (ароматы / пользователи)
2. Таблица ароматов
3. CRUD-операции
4. Форма редактирования
5. Список пользователей
6. Футер

**Динамика:**
- `POST /api/perfumes`
- `PATCH /api/perfumes/:id`
- `DELETE /api/perfumes/:id`

---

## Схема данных и API

### Модель User (таблица: users)

| Поле | Тип | Описание |
|:---|:---|:---|
| id | INT, PK, AUTO_INCREMENT | Уникальный идентификатор пользователя |
| login | VARCHAR, UNIQUE | Логин пользователя (уникальный) |
| password | VARCHAR | Хешированный пароль (bcrypt) |
| phone | VARCHAR, UNIQUE | Номер телефона (уникальный) |
| role | ENUM('user', 'admin') | Роль пользователя |
| favorites | JSON | Массив ID избранных ароматов |
| created_at | TIMESTAMP | Дата регистрации |
| updated_at | TIMESTAMP | Дата последнего обновления |

**Особенности модели:**
- Пароль хранится только в виде bcrypt-хеша
- Логин и телефон уникальны
- Роль определяет доступ к админ-панели
- Избранное хранится в JSON (без отдельной таблицы)

---

### Модель Perfume (таблица: perfumes)

| Поле | Тип | Описание |
|:---|:---|:---|
| id | INT, PK, AUTO_INCREMENT | Уникальный идентификатор аромата |
| name | VARCHAR | Название аромата |
| brand | VARCHAR | Название бренда |
| gender | ENUM('female', 'male', 'unisex') | Половая принадлежность |
| release_year | INT | Год выпуска аромата |
| rating | DECIMAL(3,2) | Рейтинг аромата |
| top_notes | JSON | Массив верхних нот |
| middle_notes | JSON | Массив нот сердца |
| base_notes | JSON | Массив базовых нот |
| description | TEXT | Описание аромата |
| country | VARCHAR | Страна |
| image_url | TEXT | Ссылка на изображение |
| image_data | LONGBLOB | Двоичные данные изображения |
| image_mime | VARCHAR(100) | MIME-тип изображения |
| volume_ml | INT | Объём флакона в миллилитрах |
| price_byn | DECIMAL(10,2) | Цена в BYN |
| created_at | TIMESTAMP | Дата добавления |
| updated_at | TIMESTAMP | Дата обновления |

**Особенности модели:**
- Информация о бренде хранится только в поле `brand`
- Ноты хранятся в JSON-полях
- Оценка хранится в поле `rating`

---

### Модель Review (таблица: reviews)

| Поле | Тип | Описание |
|:---|:---|:---|
| id | INT, PK, AUTO_INCREMENT | Уникальный идентификатор отзыва |
| perfume_id | INT | ID аромата |
| user_id | INT | ID пользователя |
| user_login | VARCHAR | Логин пользователя (для отображения) |
| rating | TINYINT | Оценка (1-5) |
| text | TEXT | Текст отзыва |
| created_at | DATE | Дата написания |

**Особенности модели:**
- Нет строгого уникального ограничения `(user_id, perfume_id)`
- Отзывы создаются через `POST /api/reviews`

---

## API Эндпоинты

### Аутентификация

| Метод | Эндпоинт | Описание | Тело запроса | Ответ |
|:---|:---|:---|:---|:---|
| POST | `/api/auth/register` | Регистрация | `{ email, password, name, phone, recoveryQuestion, recoveryKeyword }` | `{ success, userId }` |
| POST | `/api/auth/login` | Вход | `{ login, password }` | `{ success, redirect }` |
| POST | `/api/auth/logout` | Выход | — | `{ success }` |
| GET | `/api/auth/check` | Проверка сессии | — | `{ authenticated, user }` |

---

### Ароматы

| Метод | Эндпоинт | Описание | Параметры | Ответ |
|:---|:---|:---|:---|:---|
| GET | `/api/perfumes` | Список ароматов | `q`, `brand`, `gender`, `year`, `sort` | Массив ароматов |

---

### Избранное

| Метод | Эндпоинт | Описание | Ответ |
|:---|:---|:---|:---|
| GET | `/api/favorites` | Список избранного | Массив ID избранных ароматов |
| POST | `/api/favorites/:perfumeId/toggle` | Добавить/убрать из избранного | `{ success, favoriteIds }` |

---

### Отзывы

| Метод | Эндпоинт | Описание | Тело запроса |
|:---|:---|:---|:---|
| POST | `/api/reviews` | Создать отзыв | `{ perfumeId, rating, text }` |
| PUT | `/api/reviews/:id` | Редактировать отзыв | `{ rating, text }` |
| DELETE | `/api/reviews/:id` | Удалить отзыв | — |

---

### Профиль

| Метод | Эндпоинт | Описание | Тело запроса |
|:---|:---|:---|:---|
| GET | `/api/auth/profile` | Данные профиля | — |
| PATCH | `/api/auth/profile` | Обновить профиль | `{ recoveryQuestion, recoveryAnswer }` |

---

### Администрирование (требуется роль admin)

| Метод | Эндпоинт | Описание | Тело запроса |
|:---|:---|:---|:---|
| POST | `/api/perfumes` | Добавить аромат | Полный объект аромата |
| PATCH | `/api/perfumes/:id` | Обновить аромат | Обновляемые поля |
| DELETE | `/api/perfumes/:id` | Удалить аромат | — |

---

## Note de Parfum — Инструкция по установке

Ниже описаны шаги для локальной настройки и запуска проекта на машине разработчика.

**Требования**
- Node.js (рекомендуется версия 16+)
- npm (идёт в комплекте с Node.js)
- MySQL сервер (или MariaDB)

**1. Клонирование репозитория**

Склонируйте репозиторий и перейдите в папку проекта:

```
git clone <repo-url>
cd Note-de-Parfum
```

**2. Установка зависимостей**

Установите npm-зависимости:

```bash
npm install
```

В `package.json` есть скрипт запуска:

```bash
npm start
# или
node server.js
```

**3. Переменные окружения (.env)**

Создайте файл `.env` в корне проекта или используйте `.env.example` (если добавите).
Пример необходимых переменных окружения:

```
PORT=3000
DB_HOST=localhost
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=note_de_parfum
SESSION_SECRET=change_this_to_a_random_string
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASS=your_smtp_password
```

Примечание: сервер использует MySQL. Создайте базу данных и пользователя заранее:

```sql
CREATE DATABASE note_de_parfum CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'your_db_user'@'localhost' IDENTIFIED BY 'your_db_password';
GRANT ALL ON note_de_parfum.* TO 'your_db_user'@'localhost';
FLUSH PRIVILEGES;
```

**4. Запуск проекта**

После настройки `.env` и базы данных запустите сервер:

```bash
npm start
```

Откройте в браузере: `http://localhost:3000` (или порт из `PORT`).

**5. Тестирование и отладка**
- Логи сервера выводятся в консоль.
- Для изменений фронтенда достаточно обновить файл в `scripts/` и перезагрузить страницу.

**6. Частые проблемы**
- Ошибка подключения к БД: проверьте `DB_HOST`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`.
- Ошибка сессий: установите `SESSION_SECRET` и перезапустите сервер.
- Почта не отправляется: проверьте настройки SMTP.

**7. Полезные команды**
- Установить зависимости: `npm install`
- Запустить сервер: `npm start`
- Запустить сервер напрямую: `node server.js`

